#include <iostream>
#include <cmath> 
using namespace std;

int main()
{
      //Integer 27
     //Дні тижня пронумеровані наступним чином: 1 - понеділок, 2 - вівторок, ..., 6 - субота, 7 - неділя. 
    //Дано ціле число K, що лежить в діапазоні 1-365. Визначити номер дня тижня для K-го дня року, якщо відомо, що цього року 1 січня було суботою.
    int A, B;
    cout << "Введіть день(1-365)";
    cin >> A;
    B = ((A - 1) % 7 + 5) % 7 + 1;
    cout<< "День тижня за номером : " << B <<endl;
    
      //Boolean23. 
     //Дано чотиризначне число. 
    //Перевірити істинність висловлювання: «Дане число читається однаково зліва направо і справа наліво».
    int Q, W, E, R, T;
    cout << "Введіть чотиризначне число : ";
    cin >> Q;

    // Отримання кожної цифри числа
    W = Q / 1000;           // Перша цифра
    E = (Q / 100) % 10;    // Друга цифра
    R = (Q / 10) % 10;    // Третя цифра
    T = Q % 10;          // Четверта цифра

    // Перевірка на однаковість чисел
    if (W == T && E == R) {
        cout << "Число читається однаково зліва направо і справа наліво - TRUE" << endl;
    } else {
        cout << "Число читається однаково зліва направо і справа наліво - FALSE" << endl;
    }
    
    // y = ... (tab.3 #7)
    double  x, tgx, num, denom, y; // Декларація дійсних змінних
    // Введення данних
    cout << "Real argument x = ";
    cin >> x;
    
    // Підрахунок
    tgx = 1/2;                                                    // Проміжна змінна
    num = log(abs(x)) / log(4) * sqrt(abs(x * sin(x) * cos(x))); // Чисельник
    denom = cos(x + 32) + 1/2 * (sqrt(x + 5));                  // Знаменник
    y = tgx * (num / denom);
    
    // Виведення результату
    cout << "Function y = " << y <<endl;
    


    return 0;
}
